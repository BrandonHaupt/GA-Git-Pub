<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Drinks</title>
    <style>
      body {
        color: blanchedalmond;
        background-color: steelblue;
      }

      h1 {
        display: flex;
        justify-content: center;
      }

      .container {
        display: flex;
        justify-content: center;
        flex-direction: row;
      }

      li {
        list-style: none;
      }

      li a {
        text-decoration: none;
      }

      ul {
        display: flex;
        flex-direction: row;
        gap: 2rem;
      }
    </style>

  </head>
  <body>
    <h1>Welcome to gitPub!</h1>
    <div class="container">
        <h2>Drinks:</h2>
        <ul>
            <% for(let i = 0; i < allDrinks.length; i++){ %>
                <% const drink = allDrinks[i] %>
               <% const drinkName = drink.name %>
               <% const capWord = drinkName.charAt(0).toUpperCase() + drinkName.slice(1) %>
                <li>
                    <a href="/drinks/<%=i %> "><%=capWord %></a>
                </li>
           <%  } %> 
        </ul>
    </div>

    <div class="container">
        <h2>Food:</h2>
        <ul>
            <% for(let j = 0; j < allFoods.length; j++){ %>
                <% const food = allFoods[j] %>
               <% const foodName = food.name %>
               <% const capFoodWord = foodName.charAt(0).toUpperCase() + foodName.slice(1) %>
                <li>
                    <a href="/foods/<%=j %> "><%=capFoodWord %></a>
                </li>
           <%  } %> 
        </ul>
    </div>
  </body>
</html>
