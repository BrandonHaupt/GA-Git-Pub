<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Drinks</title>
    <style>
      body {
        color: blanchedalmond;
        background-color: steelblue;
      }

      h1 {
        display: flex;
        justify-content: center;
      }

      .container {
        display: flex;
        justify-content: center;
        flex-direction: row;
      }

      li {
        list-style: none;
      }

      li a {
        text-decoration: none;
      }

      ul {
        display: flex;
        flex-direction: row;
        gap: 2rem;
      }
    </style>

  </head>
  <body>
    <h1>Welcome to gitPub!</h1>
    <div class="container">
        <ul>
            <% allDrinks.forEach(drink => { %>
               <% const drinkName = drink.name %>
               <% const capWord = drinkName.charAt(0).toUpperCase() + drinkName.slice(1) %>
                <li>
                    <a href="#"><%=capWord %></a>
                </li>
           <%  }) %> 
        </ul>
    </div>
  </body>
</html>
